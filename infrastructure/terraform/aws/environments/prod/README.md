# AWS Production Environment

This directory is reserved for the deployable production root module.

Production must use state and deployment credentials isolated from development, protected approvals, restricted apply permissions, and a tested recovery procedure. Promote reviewed module versions from development; do not copy unmanaged resources or development state into this directory.

No active production Terraform configuration exists yet.
