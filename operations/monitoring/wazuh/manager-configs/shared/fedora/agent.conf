<!-- Wazuh Centralized Configuration — Fedora Group
     Agent local ossec.conf is authoritative for this host.
     This file is intentionally minimal — adding duplicate syscheck/localfile/sca blocks
     here causes double-monitoring. All config lives in agent-configs/ossec.conf.

     Manager role: sync cis_fedora_custom.yaml to /var/ossec/etc/shared/ so the agent
     can reference it as etc/shared/cis_fedora_custom.yaml in its local ossec.conf. -->
<agent_config>
</agent_config>
